{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug eth-contract-info-api",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/server",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "INFURA_NETWORK": "sepolia",
        "PORT": "8080"
      }
    }
  ]
}
